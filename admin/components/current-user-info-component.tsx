/*
import useAuthenticated from 'admin-bro';
import { ActionProps } from 'admin-bro';

const CurrentUserDetails = (props:ActionProps) => {
  
  const { email, title, avatar } = useAuthenticated;

  return (
    <div>
      <h2>Current User Details</h2>
      <p>Email: {email}</p>
      <p>Title: {title}</p>
      <img src={avatar} alt="User Avatar" />
    </div>
  );

};

export default CurrentUserDetails;
*/